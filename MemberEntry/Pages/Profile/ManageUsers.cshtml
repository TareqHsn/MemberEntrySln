@page
@model MemberEntry.Pages.Profile.ManageUsersModel
@{
}

<h2>Manage Users</h2>
<table class="table">
    <thead>
        <tr>
            <th>Email</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Roles</th>
            <th>Permissions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            <tr>
                <td>@user.Email</td>
                <td>@user.FirstName</td>
                <td>@user.LastName</td>
                <td>@string.Join(", ", user.Roles)</td>
                <td>@string.Join(", ", user.Permissions)</td>
                <td>
                    <a asp-page="/Profile/AssignPermissions" asp-route-userId="@user.Id" class="btn btn-sm btn-primary">Assign Permissions</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-page="/Profile/CreateUser" class="btn btn-primary">Create New User</a>
